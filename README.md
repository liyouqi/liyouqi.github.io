# liyouqi.github.io
My blog space


# 1. 首次运行需要安装依赖
bundle install

# 2. 启动本地服务器（开发模式）
bundle exec jekyll serve

# 或者启用实时重载
bundle exec jekyll serve --livereload

# 或者监听其他端口
bundle exec jekyll serve --port 4001